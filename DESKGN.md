# Claude 官方前端设计基线

本文档整理自 Anthropic / Claude 官方资料，作为本项目后续 UI 和交互工作的设计参考。

- 适用对象：本项目 Atelier 图片生成工作台，以及后续新增的页面、组件和状态。
- 资料范围：Claude Platform Docs 中的设计提示，以及 Anthropic 官方 Claude Code frontend-design skill。
- 检索日期：2026-07-16。
- 文件名：按需求保留为 DESKGN.md。

## 文档使用方式

本文档分为两层：

- 官方资料层：总结 Claude 文档中明确提到的默认风格、设计方法和质量底线。
- Atelier 决策层：结合本项目代码和内部参考规范，落成可以直接执行的视觉、组件和交互规则。

官方资料层用于解释为什么这样设计，Atelier 决策层用于判断具体 CSS 和组件是否合规。两者发生冲突时，产品任务和可用性优先；项目级决策不能被误读为 Anthropic 的品牌规范。

## 先说边界

Claude 官方资料这里提供的是模型的默认设计倾向和设计方法，不是一套强制的 Anthropic 品牌视觉规范，也不是所有产品都必须复制的固定主题。

特别需要注意：

- “Design and frontend defaults” 位于 Claude Opus 4.8 的专属提示文档中，描述的是该模型的默认输出倾向。
- 具体项目 brief、产品属性、用户和业务约束优先级更高。
- 颜色、字体、圆角、布局等具体值必须服务于产品主题，不能因为它们出现在官方示例中就机械复用。
- 当前项目是图片生成工作台，属于工具型界面，不应直接套用适合 editorial、酒店或作品集的展示型默认风格。

## 一、官方默认设计倾向

Claude Opus 4.8 的官方文档明确提到，它有一个比较稳定的默认 house style：

- 暖奶油色或米白背景，参考值约为 #F4F1EA。
- 衬线展示字体，示例包括 Georgia、Fraunces、Playfair。
- 适量使用斜体文字作为强调。
- 使用 terracotta 或 amber 作为强调色。
- 这套风格在 slide deck 和 web UI 中都会自然出现。

官方同时指出，这套风格适合 editorial、hospitality、portfolio 等 brief，但放在 dashboard、开发工具、金融、医疗或企业应用中可能会显得不合适。

默认风格具有持续性。只说“不要奶油色”或“做得干净、极简”，通常只会把模型推向另一套固定的默认主题，而不一定得到真正有变化的设计。

## 二、如何打破默认

官方文档给出两种可靠方式。

### 1. 给出具体替代方向

不要只给抽象形容词，要把视觉方向写成可执行的约束：

- 指定完整的色彩气质，例如冷银灰、雾化金属、蓝灰到近黑。
- 说明这种色彩应如何贯穿页面，而不是只给一个按钮的 accent。
- 指定版式结构，例如水平分区、居中的最大宽度容器和宽松留白。
- 指定圆角、间距、字体性格和字重。
- 指定图片、产品主体或主要内容在页面中的位置。
- 明确交互反馈应该克制、快速、可预测。

一个具体的设计说明比“不要默认风格”更有效。官方示例还使用了 4px 圆角、160ms 的轻量 hover 过渡和一组冷色调 hex 值，但这些只是替代方向中的示例，不是全局规定。

### 2. 先提出多个方向，再实现

在开始写代码前，先让 Claude 提出 4 个不同的视觉方向，每个方向只需要包含：

- 背景色 hex。
- 强调色 hex。
- 字体组合。
- 一句话说明为什么适合当前 brief。

让用户选择一个方向后再实现。这样比反复要求“再换一种风格”更容易得到真正不同的设计。

## 三、前端设计核心原则

以下原则来自 Anthropic 官方 frontend-design skill，作为本项目的默认工作方式。

### 1. 先从主题出发

设计前必须明确：

- 产品或内容到底是什么。
- 主要用户是谁。
- 页面只有一个最重要的任务是什么。
- 主题自身有哪些材料、工具、术语、工艺或视觉语言。

界面应该使用真实主题中的内容和语汇，而不是用一套与产品无关的装饰来填充页面。

### 2. Hero 是主题陈述

对于展示型页面，首屏应该先展示主题世界中最有代表性的内容，可以是：

- 具有明确观点的标题。
- 真实产品图或主体图。
- 动画或交互瞬间。
- 可直接操作的 demo。

大数字、渐变 accent、三栏卡片等都只是选项，不应因为它们常见就默认使用。

对于 Atelier 这类工作台，首屏的等价物不是营销 hero，而是直接进入生成工作区：Prompt、模型和生成结果应该比装饰性介绍更优先。

### 3. Typography 要承担个性

- 展示字体、正文和 utility 字体应该有明确分工。
- 字体组合应根据主题选择，不要每个项目都重复同一套。
- 建立清晰的字号层级、字重、字宽和间距。
- 字体处理本身应成为设计的一部分，而不是中性的内容容器。
- 避免无理由地使用过度常见的 Inter、Roboto、Arial 或系统字体组合。

本项目当前的 Fraunces、Outfit 和 JetBrains Mono 已经分别承担展示、正文和数据/代码三个角色。除非产品方向改变，不要只为了追求新鲜感而替换。

### 4. 结构必须传达信息

编号、眉题、分隔线、标签和其他结构元素应该表达真实关系，而不是单纯装饰。

- 只有内容确实是流程、步骤或时间线时，才使用 01 / 02 / 03 这类编号。
- 分组、顺序、层级和留白应帮助用户比较、理解和行动。
- 控件分区应对应用户任务，例如输入、参数、生成、结果和历史。

### 5. 动效要有明确目的

动效可以服务于：

- 页面进入时的关键内容揭示。
- 滚动进入时的层级提示。
- hover 或 focus 时的交互反馈。
- 能代表产品主题的环境氛围。

优先设计一个完整、有节奏的动效时刻，不要在每个元素上堆叠零散动画。过多无意义的动效反而会增加模板感。

### 6. 复杂度要匹配方向

- maximalist 方向需要完整、精细的执行，不能只增加装饰。
- minimalist 方向需要靠间距、字体和细节质量成立。
- 设计的优雅来自把选定方向执行好，而不是同时加入所有视觉技巧。

### 7. 只保留一个主要记忆点

先确定一个能代表 brief 的 signature element，再让其他部分安静、克制地支持它。

本项目可以把生成结果画廊或一张被选中的图片作为主要视觉焦点。Prompt、参数和设置应该清晰有效，不应与结果区域争夺注意力。

## 四、避免的默认审美

Anthropic 官方 frontend-design skill 提醒，AI 生成的设计容易聚集到以下三类默认外观：

1. 暖奶油背景、高对比衬线展示字体、terracotta accent。
2. 近黑背景、单一亮酸绿或 vermilion accent。
3. 报纸版式、细线、零圆角、密集多栏。

这三类风格都可能适合特定 brief，但它们首先是默认选项，而不是设计决策。只有在主题确实支持时才使用。

另外，官方提示 Claude 避免以下 generic AI aesthetics：

- 没有主题依据的模板化布局。
- 没有产品特征的卡片堆叠。
- 陈旧或过度重复的字体组合。
- 尤其是紫色渐变配白色或深色背景的通用方案。
- 与内容无关的装饰、动效和标签。

本项目应优先保持工作台的可扫描性和工具感，而不是为了“看起来像 AI 作品”去增加装饰。

## 五、视觉 token 规则

每个新页面或明显改版前，先写一份小型 token system。官方 skill 建议至少包含以下四项。

### Color

每个方向使用 4 到 6 个有名字的颜色，并写明角色：

- 页面背景。
- 面板或内容表面。
- 主文本和次文本。
- 主强调色。
- 成功、警告、错误等语义色。

颜色应该形成完整系统，不要只挑一个漂亮的 accent。全页面的色调应与主题一致。

### Type

至少定义两个角色，必要时增加第三个：

- Display：标题或品牌识别。
- Body：正文、标签和说明。
- Utility：数据、代码、元信息或状态。

为每个角色说明字体、字号范围、字重、行高和使用边界。

### Layout

用一句话描述整体布局，并用简单 wireframe 先比较方案。例如：

    [生成输入与参数]  |  [结果画廊与预览]
    [历史与状态]      |  [保存 / 加入编辑 / 删除]

布局应先回答用户如何完成任务，再决定视觉装饰。

### Signature

每个方向只选择一个独特记忆点。它可以是结果图的聚焦方式、某种内容编排、一个有主题依据的交互动作，或一种与产品材料相关的视觉语言。

## 六、Atelier 的落地建议

以下内容是基于本项目 README 和现有界面整理的项目适配建议，不是 Anthropic 的官方要求。

### 产品定位

- 产品：本地持久化的 JS 全栈文生图和图片编辑客户端。
- 用户：需要反复调整 Prompt、模型和参数并比较图片结果的创作者或开发者。
- 单一核心任务：尽快生成、检查并继续处理一张图片。

### 信息层级

1. 生成模式和 Prompt。
2. 当前生效的 Provider 与模型。
3. 尺寸、数量、质量和其他参数。
4. 生成中的状态与错误。
5. 结果画廊和图片操作。
6. 设置、历史和高级透传参数。

### 设计方向

- 保持工具型工作台的密度和可扫描性，不直接复制暖奶油 editorial 默认。
- 让图片结果成为最强视觉焦点，控制区使用稳定、低干扰的表面。
- 保留 Fraunces / Outfit / JetBrains Mono 的角色分工，避免无意义地改字体。
- 颜色应服务于状态和层级：主操作、成功、错误、次要信息必须容易区分。
- 不使用与图片生成无关的装饰性编号、标签或动效。
- 首屏直接进入工作区，不额外加入营销式介绍。

### 交互语言

- 模式使用用户能理解的词，例如“文生图”和“图片编辑”。
- 按钮写明动作，例如“生成图片”“保存 Provider”“清空结果”。
- 同一动作在流程中保持同一个名称。
- 加载、空状态、错误和成功都要告诉用户当前发生了什么，以及下一步怎么做。

### 项目级视觉决策

以下规则参考了本地的 Scraper Console DESIGN.md，并按 Atelier 的原生 JS 图片工作台结构重新取舍。它们是本项目的实施决策，不是 Claude 官方强制规范。

#### 视觉基调

- 基调是 Claude 温调、书斋感和工具秩序：适合长时间使用，信息密度可观，但不冷酷。
- 当前产品采用单一深色主题，不新增浅色切换，除非产品需求明确改变。
- 新增页面不使用背景渐变、装饰性图形或与图片生成无关的氛围插画。图片本身就是主要视觉资产。
- 结构主要靠留白、字号和层级建立，少用连续分隔线和嵌套卡片。
- 不使用大圆角来制造“柔软感”。圆角服务于控件分组和点击边界。
- 已有样式中的渐变和大圆角属于历史实现；后续改版逐步收敛，不为无关重构扩大范围。

#### 色彩 token

继续使用 public/css/style.css 中已有的语义变量，不在组件里随意写新颜色：

| 角色 | 当前 token | 当前值 | 使用规则 |
|---|---|---|---|
| 页面背景 | ink | #12100E | 整体工作区底色 |
| 面板表面 | surface-solid | #1E1B17 | 表单、设置和内容面板 |
| 提升表面 | ink-4 | #2E2A24 | hover、选中和局部强调 |
| 主文字 | text | #F4ECDF | 标题、主要字段和结果信息 |
| 次要文字 | text-dim | #B5A992 | 标签、说明和辅助信息 |
| 弱化文字 | text-faint | #7D7364 | 元信息和非主操作提示 |
| 主强调 | brass / brass-2 | #D4A35C / #E8C07A | 主要操作、focus 和关键状态 |
| 成功 | moss | #8FBC8F | 成功、已保存和可用状态 |
| 警告或危险 | coral | #E08B72 | 错误、删除和需要注意的状态 |
| 描边 | line / line-strong | rgba(245,230,205,0.1 / 0.18) | 低对比结构边界 |

颜色必须从这些语义 token 派生。需要增加颜色时，先说明它承担的语义，不为单个卡片临时取色。若未来迁移到 OKLCH，保留上述变量名称和语义，不改变组件调用方式。

#### 字体与字号

| 角色 | 字体 | 默认范围 | 用途 |
|---|---|---|---|
| Display | Fraunces | 18-28px | Atelier 品牌、页面标题、结果区标题 |
| Body | Outfit | 14-16px | 表单、按钮、说明和正文 |
| Utility | JetBrains Mono | 11-13px | Provider、模型名、JSON、尺寸和时间 |

- 字重以 400、500、600 为主，700 只用于真正重要的数值或标题。
- 标签、状态和元信息可以使用 11-12px，但必须保持对比度和可读性。
- 新样式不使用负字距；字距应由字号、字重和留白解决。
- Display 字体只表达品牌和层级，不用于所有控件。

#### 间距、圆角与阴影

- 间距优先使用 4、8、12、16、24、32px，避免为了局部对齐引入大量特殊值。
- Button、Input、Select 和 Badge 使用 6px 左右圆角。
- Panel、Card、Dialog 和 Popover 使用不超过 8px 的圆角。
- Pill、状态点和头像可以使用全圆角。
- 不使用 16px、20px 或更大的默认圆角；除非一个视觉对象本身就是明确的圆形或胶囊。
- 普通 Card 默认只使用低对比 border，不使用明显 shadow；Popover、Dialog 和图片预览可以使用很轻的暖色阴影。
- 阴影用来表达浮层关系，不用来装饰每一个面板。

#### 工作区布局

- 首屏直接进入生成工作区，左侧负责模式、Prompt、模型和参数，右侧负责状态、结果画廊和图片操作。
- 结果画廊是主视觉焦点；参数面板保持稳定、紧凑和可扫描。
- 桌面宽度使用有限的最大宽度，避免图片和文本无限拉伸；当前应用保持 1600px 左右的上限。
- 只保留有意设计的滚动容器：参数面板和结果区可以独立滚动，不能再层层增加隐藏的 overflow 包裹。
- 不把一个完整卡片嵌套到另一个卡片里。需要分组时优先使用标题、留白和低对比描边。

响应式处理：

| 宽度 | 处理 |
|---|---|
| 小于 900px | 参数面板和结果区纵向排列，操作按钮允许换行 |
| 900-1280px | 保持双栏，压缩参数栏和面板内边距 |
| 1280-1600px | 使用标准双栏比例，结果区获得主要空间 |
| 大于 1600px | 主区居中，不无限拉宽 |

#### 组件决策

- Button 最小高度 36px；常用 icon button 最小 32px，并提供 aria-label。
- Button、Input 和 Select 的文本使用 14px 左右，标签使用 12-13px。
- 主要操作只有一个视觉主按钮；“清空”“删除”等破坏性动作使用明确的危险色。
- Badge 用于状态，不用来堆叠装饰性标签。状态颜色必须配合文字或图标，不能只靠颜色。
- Tabs 使用轻量下划线或低对比底色，不使用厚重的胶囊式导航。
- Modal、Popover 和 Dropdown 只承载当前任务需要的内容，关闭和返回路径始终清晰。
- 图片操作使用熟悉的图标；不熟悉的图标提供 tooltip，所有图标按钮都保留可访问名称。
- Loading 优先使用 spinner、骨架或顶部进度条，不使用无意义的 shimmer。
- Empty state 由一个简短说明和下一步动作组成，不用大面积插画占位。

#### 动效决策

| 场景 | 时长 | 曲线 |
|---|---|---|
| hover / focus | 120ms | ease-out |
| 内容进入 | 180ms | cubic-bezier(0.16, 1, 0.3, 1) |
| 内容退出 | 120ms | ease-in |
| Dialog / Modal | 250ms | cubic-bezier(0.16, 1, 0.3, 1) |
| Tab 切换 | 120ms | ease-out |

- 禁止 bounce、装饰性 rotate 和无意义 shimmer。
- 动效只表达状态变化、层级变化或生成结果出现，不用于填充空白。
- prefers-reduced-motion 开启时，关闭非必要的进入和环境动效。

#### 无障碍底线

- 所有可交互元素都有清晰可见的 focus ring。
- 图标按钮必须有 aria-label；只提供图标时不能让用户猜测含义。
- 正文和主要控件的文本对比度至少达到 4.5:1。
- 状态不能只通过颜色、透明度或动画表达。
- 键盘可以完成模式切换、Prompt 输入、生成、关闭弹层和图片操作。
- Tooltip 只补充信息，不能成为完成核心操作的唯一途径。

#### 不直接复制的部分

Scraper Console DESIGN.md 中的 shadcn Sidebar、React 组件树、Tailwind 类名、Command Palette 快捷键和“不做移动端”判断不适用于当前项目。Atelier 继续使用现有的原生 HTML、CSS 和 JavaScript 结构，只吸收其中的决策方法、暖调系统、密度控制、状态设计和质量检查方式。

## 七、文案规则

官方 frontend-design skill 把文字视为设计材料，而不是装饰。

### 命名

- 从用户看到的内容和控制对象命名，而不是从内部实现命名。
- 使用具体、可识别的名词。
- 说明功能做什么，不要用营销话术替代说明。

### 动作

- 默认使用主动语态。
- 按钮要准确说明执行的动作，例如“保存更改”，不要用含糊的“提交”。
- 一个动作在按钮、确认、toast 和结果状态中使用一致词汇。

### 错误与空状态

- 错误说明发生了什么，并给出可执行的修复方向。
- 不使用含糊的“出错了”作为唯一信息。
- 错误文案不需要替产品道歉。
- 空状态应该是下一步的入口，例如提示配置 Provider、填写 Prompt 或开始生成。
- 标签、示例和说明各自只承担一个职责。

### 语气

- 使用简洁、自然、句首字母正常的句式。
- 少用填充语和过度解释。
- 语气匹配产品和用户，不为追求“聪明”而牺牲清晰度。

## 八、可访问性与响应式底线

官方 skill 把以下内容列为质量底线：

- 页面至少适配移动端。
- 键盘操作时必须有清晰可见的 focus 状态。
- 尊重 prefers-reduced-motion，减少或关闭非必要动效。
- 文本、控件和动态内容不能互相遮挡。
- 交互状态不能只依赖颜色或动效表达。
- 空状态、加载状态、成功状态和错误状态都要有明确的可读文本。

## 九、推荐工作流

### 1. Brainstorm

先写下产品主题、用户、单一任务和约束。不要马上开始写 CSS。

### 2. Explore

提出 3 到 4 个视觉方向，至少包含背景色、强调色、字体组合和一句理由。检查它们是否真的不同。

### 3. Plan

为选中的方向写出：

- 4 到 6 个颜色 token。
- 2 到 3 个字体角色。
- 布局描述和 wireframe。
- 一个 signature element。
- 关键空状态、错误状态和加载状态。

### 4. Critique

实现前检查：

- 这个方向是否只因为模型熟悉而出现。
- 如果换一个主题，是否仍然会得到同样的方案。
- 每个装饰是否都服务于主题、层级或操作。
- 是否把“默认风格”误当成“官方规范”。

### 5. Build

严格按选定的 token 和布局执行。控制 CSS 选择器的作用域，避免通用选择器和组件选择器互相覆盖。

### 6. Critique again

实现后用截图检查桌面和移动端，重点看：

- 结果焦点是否足够明确。
- 文本和控件是否溢出或重叠。
- focus、hover、loading、empty、error 状态是否完整。
- 动效是否有必要，是否尊重 reduced motion。
- 是否还能删掉一个不必要的装饰。

## 十、提交前检查清单

- [ ] 已写清产品主题、用户和单一核心任务。
- [ ] 视觉方向有具体颜色和字体，而不是只有“现代、干净、极简”等形容词。
- [ ] 已确认没有无理由复制 Claude 的暖奶油默认风格。
- [ ] 页面有明确的信息层级和唯一主要视觉焦点。
- [ ] 展示、正文和 utility 字体角色清楚。
- [ ] 结构元素表达真实信息，不是装饰性编号。
- [ ] 按钮和状态文案使用一致的动作词。
- [ ] 空状态、加载、成功和错误都能指导下一步。
- [ ] 桌面和移动端都检查过，没有文字或控件重叠。
- [ ] 键盘 focus 可见。
- [ ] reduced motion 已处理。
- [ ] 已截图做过一次实现后的自我批评。

## 参考来源

1. Anthropic, “Prompting Claude Opus 4.8”, section “Design and frontend defaults”
   https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-opus-4-8#design-and-frontend-defaults

2. Anthropic Claude Code, official frontend-design skill
   https://github.com/anthropics/claude-code/blob/main/plugins/frontend-design/skills/frontend-design/SKILL.md

3. 本地项目参考：Scraper Console 前端视觉设计规范
   ../scraper-console/web/DESIGN.md

如果 Claude 文档中的模型版本或 frontend-design skill 发生变化，应重新核对上述来源，再更新本文件中的版本说明和默认行为。
